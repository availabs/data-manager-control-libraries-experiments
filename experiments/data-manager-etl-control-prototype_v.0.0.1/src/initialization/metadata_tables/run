#!/usr/bin/env node

require("ts-node").register();

const { default: create } = require("./createMetadataTables");
const { default: load } = require("./loadMetadataTables");

function main() {
  create();
  load();
}

main();
